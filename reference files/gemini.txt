/* Example Media Query - Put this in your responsive.css */
@media (max-width: 900px) {
    /* 1. Show the mobile menu button */
    .toggle-btn {
        display: block;
    }
    
    /* 2. Hide the desktop menu */
    .navmenu {
        display: none;
    }
    
    /* 3. Style the Dropdown Menu for mobile */
    .dropmenu {
        /* This is the default closed state */
        position: absolute;
        top: 8rem; /* Adjust based on your header height */
        right: 10%;
        height: 0; /* Starts collapsed */
        transition: height 0.3s ease;
        overflow: hidden;
    }

    /* 4. Use the JS .open class to expand the menu */
    .dropmenu.open {
        height: 15rem; /* Set to the height needed to show all links */
    }

    /* 5. Fix Slider Height for Mobile */
    .ahm-slider .slides {
        height: 80vh; /* Use viewport height for better mobile scaling */
    }
}





<figure class="f-card">
    <img src="..." alt="...">
    <button class="ahm-accordion">Graphic Designs</button> 
    <figcaption class="panel">
        <p>Logo</p>
        </figcaption>
</figure>




/* --- ACCORDION BASE STYLES --- */

/* 1. Hide the panel initially and prepare for transition */
.panel {
    /* Use max-height with overflow: hidden for the sliding effect */
    max-height: 0; 
    overflow: hidden;
    transition: max-height 0.4s ease-out, opacity 0.4s ease-out;
    opacity: 0; /* Fade out content while collapsed */
}

/* 2. Style the accordion button */
.ahm-accordion {
    /* Style this to look like a button/link */
    display: block;
    width: 100%;
    cursor: pointer;
    text-align: left;
    /* ... add your specific button styling (background, border, padding) */
}

/* --- HOVER EFFECT: Reveal the panel when the CARD is hovered --- */
.f-card:hover .panel {
    /* When the parent card is hovered, expand the panel */
    max-height: 500px; /* Use a value larger than the max height of the content */
    opacity: 1; /* Fade in content */
}

/* --- CLICK EFFECT: Keep the panel open after it's clicked (JS) --- */

/* Add this to style the accordion button when the 'active' class is toggled by JS */
.ahm-accordion.active {
    /* Optional: change button color/background when active */
    /* background-color: var(--accent-color); */
}

/* Also ensure the panel stays open when the button has the active class */
.ahm-accordion.active + .panel {
    max-height: 500px; 
    opacity: 1;
}




https://gemini.google.com/app/58c01e6058fcffcb?utm_source=gemini&utm_medium=email&utm_campaign=assistant_upgrade_welcome_opt_in

https://gemini.google.com/app



?
A Note From the Founders

Loro Labs began with a simple goal: to make tech feel less like a barrier and more like a bridge. Whether it’s learning a language or launching an idea, the right tools can make all the difference.
We build with empathy, creativity, and purpose—to help you move forward.



logo
Yours,
The Loro Labs Team
